/* Root container */
.resource-history-modal-root {
    flex-grow: 0;
    flex-shrink: 0;
    padding: 0;
    width: 400px;
    height: 600px;
}

/* Scroll view */
.resource-history-scroll {
    flex-grow: 1;
    align-items: center;
}
